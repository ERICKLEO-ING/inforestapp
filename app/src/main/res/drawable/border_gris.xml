<!-- res/drawable/border_edit_text.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo blanco -->
    <!--    <solid android:color="@android:color/white" />-->
    <stroke
        android:width="1dp"
        android:color="@color/lightgray_600" /> <!-- Borde de color y grosor -->
    <corners android:radius="10dp" /> <!-- Esquinas redondeadas opcionales -->
</shape>